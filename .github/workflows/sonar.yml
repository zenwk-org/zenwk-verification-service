name: SonarCloud

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:   #Habilita ejecución manual desde Actions

jobs:
  sonar:
    uses: zenwk-org/infra-zenwk/.github/workflows/sonarcloud.yml@main
    with:
      working-directory: ./zenwk-verification
    secrets: inherit